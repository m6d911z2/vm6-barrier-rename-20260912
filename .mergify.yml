commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  mode: serial
  max_parallel_checks: 1
queue_rules:
  - name: default
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/vm6-synthetic-head-delay
    max_checks_retries: 0
    merge_method: merge
    checks_timeout: null
merge_protections:
  # Migrated from post_check rule 'VM6 post-check same-SHA context discriminator'. Dropped options with no equivalent: title, summary
  - name: VM6 post-check same-SHA context discriminator
    if:
      - title = VM6 POSTCHECK SAME SHA CONTEXT 20260915
    success_conditions:
      - base = vm6-postcheck-base-a-20260915
