pull_request_rules: []
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  mode: serial
  max_parallel_checks: 1
queue_rules:
  - name: default
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/vm6-synthetic-head-delay
    max_checks_retries: 0
    merge_method: merge
    checks_timeout: null

merge_protections:
  - name: VM6 review state freshness current 20260917
    if:
      - base = vm6-review-current-target-20260917
    success_conditions:
      - '#approved-reviews-by >= 1'
merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
